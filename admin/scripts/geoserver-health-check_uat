#!/bin/bash
result=`wget http://uat.dga.links.com.au/geoserver/web/wicket/bookmarkable/org.geoserver.web.AboutGeoServerPage --timeout 30 -O - 2>/dev/null | grep "About GeoServer"`
if [[ -z "${result// }" ]]; then
        service tomcat stop
        service tomcat start
fi